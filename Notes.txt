AOD v1.3

Change the link to player function to be automatic when editing or adding player rather than a separate manual task.


TabBarView lazy loading — Both tabs in GameRosterScreen build immediately. Wrapping the body in IndexedStack or using AutomaticKeepAliveClientMixin .hould prevent the available-players list from re-rendering every tab switch.

Pagination on players list; use .range() with an infinite scroll ListView.

